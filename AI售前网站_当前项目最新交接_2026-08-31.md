# AI 售前网站｜当前项目交接（V2）

> 本文基于本轮完成后的仓库状态整理；当前架构的详细可信来源为 `docs/ARCHITECTURE_V2.md`。旧 Phase 3.5 交接仅作历史资料。

## 当前项目

这是一个面向 AI 售前 / 解决方案工程师求职场景的作品集。现有页面：

- `/`：首页项目入口
- `/resume`：在线简历
- `/demo/tea-assistant`：一叶春山 AI 导购 / 客服知识库 POC
- `/demo/tender-agent`：AI 招投标与方案生成 Agent

Demo1 与 Demo2 使用逻辑隔离的数据空间。茶叶 Demo 继续使用 `data/tea/**` 与既有 RAG；Tender Agent 只读取 `data/tender/**` 的 Synthetic Demo Data。

## Tender Agent 已实现闭环

1. Sample 招标文件与 TXT / Markdown 上传输入
2. 服务端 Document Parser：项目基本信息、资格、技术、交付和评分规则抽取，并保留文件位置
3. DeepSeek Structured Planner：配置服务端 Key 时，模型在允许工具列表中决定检索动作；失败时使用规则 Planner fallback
4. Tool Registry：`search_company_qualification`、`search_product_capability`、`search_historical_cases`、`search_external_web`（未配置）
5. 逐条资格匹配与偏离分析：PASS / PARTIAL / MISSING / UNKNOWN，及 LOW / MEDIUM / HIGH 风险
6. 透明的 Tool Calling：输入摘要、结果摘要、来源、状态和耗时；不显示内部推理
7. 技术响应建议、人工复核和 Markdown 基础导出

## 当前边界

- 所有企业、资质、案例、产品能力都是 **Synthetic Demo Data / POC 示例**。
- TXT / Markdown 是真实解析；PDF / Word / 扫描文件仅有 OCR interface，尚未接入真实二进制解析或 OCR 服务。
- Web Search adapter 已预留，但未接入 provider；不会以外部搜索判断企业内部事实。
- Agent 的结论仅辅助售前分析。资格有效性、案例、承诺和最终投标决策必须由人工确认。

## 验证命令

```bash
npm run lint
npx tsc --noEmit
npm run test:tea
npm run test:rag
npm run test:tender
npm run build
```

Tender POC 测试覆盖 10 类场景：正常文本、资料不足、硬性条件未知、多技术要求、兼容性边界、未知能力、Prompt Injection、LLM 不可用、空文件、OCR 文本转写 fallback。

## 下一步

Phase 6 可补充两项目架构图、可视化数据流、POC 报告、失败案例展示、演示脚本与方案/PPT 大纲；Phase 7 完成部署与最终验收。
