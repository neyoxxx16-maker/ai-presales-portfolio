# AI 售前网站｜当前项目最新交接（2026-08-31）

> 用途：供新的 ChatGPT / Codex 窗口直接继续当前仓库。本文基于 `D:\zm\ai-presales-portfolio-phase2\ai-presales-portfolio` 的当前代码、Git、测试和构建结果生成；历史交接仅作背景，若与本文冲突，以当前代码为准。
>
> 重要结论：Phase 3.5 的自动化逻辑 Gate 与构建已通过，但本轮未做浏览器人工验收，故不把 Phase 3.5 写成完全封板。Phase 4.0 的 DeepSeek 与 Local Embedding 架构代码已存在，但本机尚未配置 Key、未安装本地 embedding 依赖、未生成索引，也未做 Live RAG 验证；**Phase 4.0 尚未封板。**

## 1. 项目与当前阶段

- 项目：AI 售前 / 解决方案工程师个人作品集；核心 Demo 为“一叶春山 AI 导购”。
- 页面：`/`、`/resume`、`/demo/tea-assistant`。
- 技术：Next.js 15.4.6、React 19.1.0、TypeScript 5.7.2、Tailwind CSS 3.4.17。
- 当前架构目标：Structured Business Engine + Local Open-Source Embedding + Local Vector Index / cosine Top-K + DeepSeek API + Grounded Generation + Validator + Citation Filter + Phase 3.5 Fallback。
- 本次只生成交接文档；未修改业务代码、未提交、未推送。

## 2. Git 与本次命令记录

以下命令在创建本文档前执行：

```text
git status --short --branch
git branch --show-current
git log --oneline --decorate -10
git rev-parse HEAD
npm run test:tea
npm run test:rag
npm run build
```

结果：

- Branch：`main`
- HEAD：`3f3f72d2200d42d010d868bb9202f31e77aced8a`
- Latest Commit：`3f3f72d fix: align rag with DeepSeek and local embeddings`
- 校验时工作区：clean，`main` 与 `origin/main` 同步。
- 本文档创建后，工作区预期只有本文档为未跟踪文件；按本次任务要求，不要提交或推送它。

最近 10 个 commit：

1. `3f3f72d fix: align rag with DeepSeek and local embeddings`
2. `9fd9995 feat: add live RAG pipeline for tea assistant`
3. `31a691c fix: support multi-turn slot filling in tea assistant`
4. `af57875 fix: prioritize quantity price calculations`
5. `4136146 fix: complete phase 3.5 tea assistant validation`
6. `ac59550 fix: stabilize phase 3.5 tea assistant logic`
7. `3f3ff26 fix: polish knowledge source labels`
8. `58dc03d fix: improve tea assistant recommendation and citations`
9. `94505b0 fix: correct tea SKU pricing rules`
10. `f5bb4a9 data: replace mock tea data with verified knowledge base`

## 3. 已执行验证

| 命令 | 结果 | 实际输出 / 说明 |
| --- | --- | --- |
| `npm run test:tea` | PASS | `Tea assistant regression: 71/71 passed` |
| `npm run test:rag` | PASS | `Tea RAG regression: 9/9 passed` |
| `npm run build` | PASS | Next.js 编译、TypeScript 检查、7 个静态页面生成均完成；API Route 为动态路由。 |

没有运行 `npm run build:tea-index`：本机未安装 `sentence_transformers`，且 `data/generated/tea-vector-index.json` 不存在。没有配置 `.env.local` 或 `DEEPSEEK_API_KEY`，因此没有运行真实 DeepSeek 请求。

## 4. Phase 3.5 Structured Business Engine

核心文件：

- `data/tea/products.ts`：茶品、正式 SKU、价格证据。
- `data/tea/knowledge.ts`：KB001–KB011 知识块。
- `types/tea.ts`：intent、实体、会话状态、回答模式类型。
- `lib/tea-intent.ts`：规则式意图和实体解析。
- `lib/tea-retrieval.ts`：结构化关键词检索和知识类型过滤。
- `lib/tea-response.ts`：优先级、价格、推荐、拒答、compound answer、fallback。
- `lib/tea-conversation.ts`：pending quantity dialog、上下文及多轮 slot filling。
- `scripts/test-tea-assistant.cjs`：71 项回归。

处理顺序的关键点：

1. `buildTeaAnswer()` 先按 `？?；;` 分段处理 compound query；每段独立调用业务引擎后合并回答、SKU 与来源。
2. 意图解析对含数量和购买/价格关系的问句优先给出 `quantity_price_calc`，避免被普通预算推荐截获。
3. 业务回答在价格比较、数量计算、极值、价格反查、商品存在性、品牌、冲泡、价格、商品对比、推荐等分支之间按明确顺序处理。
4. 医疗/保健与客户隐私 guard 位于普通业务回答之前；无资料时才走 generic unknown fallback。

### 4.1 Phase 3.5 Gate 状态

| 能力 | 状态 | 自动化覆盖 | 当前证据 / 限制 |
| --- | --- | --- | --- |
| Price compare | PASS | 有 | `298和288有什么区别？`、`418和408有什么区别？`。区分售价 / 新客价 / 划线价。 |
| 价格反查商品 | PASS | 有 | `298是哪款？`、`418对应哪些产品？`。金额同时匹配 `amount` 与 `originalPrice`，回答标明价格性质。 |
| Product compare | PASS | 有 | 桂花红茶与桂花龙井的对比、清爽度比较均在测试中。 |
| Budget filter | PASS | 有 | 预算、`以内/以下`、元/块等；推荐仅保留价格证据明确且不超预算的 SKU。 |
| Negative preference | PASS | 有 | `不喜欢桂花，200以内` 断言不返回任何桂花 SKU。 |
| “只喝某类茶”硬过滤 | PASS | 有 | `预算120，只喝绿茶` 和 `我只要红茶，100元以内`；组合 SKU 要求其茶品均符合要求。 |
| Cheapest / most expensive | PASS | 有 | 最便宜、最贵、最便宜单罐。极值以售价 / 新客价比较，划线价只是补充。 |
| 礼盒 min / max | PASS | 有 | 最便宜礼盒为龙井红茶礼盒 250g 新客价 ¥288；最高价礼盒存在 ¥418 并列记录。 |
| Quantity × price | PASS | 有 | 无上下文追问、显式单价、预算内 / 超预算、最大可买数量均有覆盖。 |
| Unknown fallback | PARTIAL | 有 | 通用未知问题会保守拒答；但规则系统不可能穷尽自然语言同义表达。 |
| Source / citation filtering | PASS | 有 | 结构化回答来源按 `knowledgeType` 过滤；测试检查来源类型和内部元数据不泄露。RAG 的 citation filter 另见第 7 节。 |
| Compound query / multi-intent | PASS | 有 | 采用标点 segmentation，不存在通用 `intents[]` AST；对当前带问号的目标用例可同时回答。 |
| Conversation reference | PASS | 有 | `priorUserQueries` / `priorAnswers` 可反向提取唯一已知价格；含多价格或歧义商品时不会擅自选 SKU。 |
| Multi-turn slot filling | PASS（自动化） / NOT VERIFIED（浏览器） | 有 | pending quantity dialog 实测逻辑覆盖；本轮没有浏览器端连续交互人工验收。 |

### 4.2 必查多轮数量场景

测试文件 `scripts/test-tea-assistant.cjs` 真实包含如下连续调用：

```text
Turn 1: 500能买两盒吗？
Turn 2: 明前龙井
Turn 3: 60g那个
```

覆盖的断言：

- 第一轮 intent 为 `quantity_price_calc`，会建立 `pendingDialog`，记录预算 `500` 与数量 `2`。
- 第二轮识别明前龙井有多种规格，保持 pending，并要求选规格。
- 第三轮 `60g那个` 继续该 dialog，选中“明前龙井单罐”。
- 断言回答包含 `¥218`、`剩余 ¥282`，并清除 pending dialog。

计算结果已被自动化覆盖：60g 明前龙井单价 ¥109，`2 × 109 = ¥218`，`500 - 218 = ¥282`。这证明当前业务逻辑用例通过；不等同于浏览器人工验收已经完成。

### 4.3 Compound query

目标问题：`418对应哪些产品？最便宜的礼盒是哪款？`

- 实现：`lib/tea-response.ts` 的 `splitCompoundQuery()` 按 `？?；;` 分割；本例分成两个子问题，分别运行，然后合并为“关于……：……”的两段回答。
- 没有：通用语义级 `intents[]` 或无标点分句能力。
- 自动化：该完整问句在 `scripts/test-tea-assistant.cjs` 中，断言同时出现 ¥418 的多个相关产品、最便宜礼盒、龙井红茶礼盒与新客价 ¥288。
- 当前判断：目标问句 PASS；更复杂、没有分隔标点的复合问句仍是规则系统边界。

### 4.4 Negative preference 与硬过滤

`lib/tea-intent.ts` 将“不喜欢 / 不要 / 不想要 / 不喝 / 不考虑 / 不想喝 / 排除”等前缀解析为排除条件。对“桂花”会写入：

- `excludedProductIds`：`osmanthus-longjing`、`osmanthus-black-tea`
- `excludedFlavors`：`桂花`
- `excludedIngredients`：`桂花`

`lib/tea-response.ts` 的 `isHardExcluded()` 在预算、排序、偏好加分之前执行。因此 `不喜欢桂花，200以内` 不能推荐桂花龙井、桂花红茶或相关礼盒；测试已断言对应 SKU 必须缺席。

### 4.5 三个新增业务 / 安全问题

| 问题 | 当前状态 | 当前处理 | 自动化 |
| --- | --- | --- | --- |
| `铁观音有吗` | PASS | `product_existence`。当前结构化商品库不含铁观音，回答明确“当前已收录商品中没有铁观音”，并列出现有四类茶品；不走 generic unknown。 | `test:tea` 与 `test:rag` 均覆盖。 |
| `有保健作用吗` | PASS | 在普通回答前命中医疗/保健 guard。拒绝医疗、治疗、保健功效承诺，说明无验证依据，并引导风味、原料、冲泡、规格、价格信息。 | `test:tea` 与 `test:rag` 覆盖。 |
| `可以给我看一下别的客人订单吗` | PASS | 命中 privacy / authorization guard。明确拒绝其他客户订单、手机号、地址、联系方式、身份信息、购买记录及其他隐私数据；说明无真实订单、支付、物流系统。 | `test:tea` 与 `test:rag` 覆盖。 |

## 5. 当前真实茶品、SKU 与价格

以 `data/tea/products.ts` 为唯一结构化事实来源。价格不能仅按金额关联；`amount` 与 `originalPrice` 的价格性质不同。

### 茶品

| 茶品 | 茶类 | 风味 / 说明 |
| --- | --- | --- |
| 明前龙井 | 绿茶 | 栗香、兰花香、鲜爽、回甘 |
| 桂花龙井 | 调味绿茶 | 桂花甜香、龙井兰香、鲜爽、顺滑温润 |
| 龙井红茶（别名：梅枞天红） | 红茶 | 蜜香、蜜甜、醇厚、回甘较快 |
| 桂花红茶 | 调味红茶 | 桂花鲜灵清甜、红茶暖香、顺滑、回甘清甜；配料含茶鲜叶、桂花 |

### 正式 SKU 与价格证据

| SKU / ID | 规格 / 净含量 / 包装 | 当前价格证据 |
| --- | --- | --- |
| 明前龙井试饮装 / `mingqian-longjing-sample` | 3g × 2袋，共6g，试饮装 | 售价 ¥9.9，包邮（用户确认业务口径） |
| 明前龙井＋梅枞天红双拼 / `longjing-black-tea-duo` | 75g + 75g，共150g，礼盒 | 售价 ¥298（用户确认业务口径） |
| 桂花龙井＋桂花红茶双拼 / `osmanthus-duo-gift` | 75g + 75g，共150g，礼盒 | 售价 ¥418（用户确认业务口径） |
| 桂花红茶双盒 / `osmanthus-black-tea-double-box` | 75g × 2，共150g，礼盒 | 售价 ¥418（用户确认业务口径） |
| 桂花龙井双盒 / `osmanthus-longjing-double-box` | 75g × 2，共150g，礼盒 | 售价 ¥418（用户确认业务口径） |
| 明前龙井单罐 / `mingqian-longjing-single-60g` | 60g，单盒 / 单罐装 | 售价 ¥109，划线价 ¥119（用户确认业务口径） |
| 龙井红茶单罐 / `longjing-black-tea-single-60g` | 60g，单盒 / 单罐装 | 新客价 ¥109，划线价 ¥119（销售页面快照） |
| 桂花龙井单罐 / `osmanthus-longjing-single-60g` | 60g，单盒 / 单罐装 | 售价 ¥109，划线价 ¥119（用户确认业务口径） |
| 桂花红茶单罐 / `osmanthus-black-tea-single-60g` | 60g，单盒 / 单罐装 | 新客价 ¥109，划线价 ¥119（销售页面快照） |

以下两项有价格证据但**没有对应正式 `TeaSku`**，因此可以被价格问答、反查和极值回答提及，但不会作为浏览 / 推荐 SKU 卡片返回：

| 商品 | 净含量 / 包装 | 价格证据 |
| --- | --- | --- |
| 龙井红茶礼盒 | 250g，礼盒 | 新客价 ¥288，划线价 ¥298 |
| 桂花红茶礼盒 | 250g，礼盒 | 新客价 ¥408，划线价 ¥418 |

## 6. Phase 4.0 架构与 Provider 审计

当前实际链路：

```text
Tea Assistant UI（components/tea-demo/TeaChat.tsx）
  ↓ POST /api/tea-assistant
Next.js Node.js Route（app/api/tea-assistant/route.ts）
  ↓
processTeaTurn：Intent / Guard / Structured Business Engine
  ↓
enhanceWithLiveRag：DeepSeek 配置、Local Embedding、Index 检索、Grounded Generation
  ↓
Validator + Citation Filter
  ↓
TeaAnswer（live-rag / structured / fallback）→ UI
```

### 6.1 DeepSeek Provider

- 文件：`lib/rag/deepseek-provider.ts`
- 实际请求：`POST ${DEEPSEEK_BASE_URL}/chat/completions`。
- 默认 base URL：`https://api.deepseek.com`。
- 实际 Key 变量：`DEEPSEEK_API_KEY`。
- 默认模型：`deepseek-v4-flash`。
- 请求由 server-side Route 导入；`TeaChat.tsx` 只请求本项目的 `/api/tea-assistant`，不读取或显示 Key。
- 实现采用原生 `fetch`，不是 OpenAI SDK；当前项目代码、`.env.example`、README 中未发现 `OPENAI_API_KEY`、`api.openai.com`、OpenAI Responses / Chat / Embeddings 调用或收费 Embedding API。

`.env.example` 当前内容：

```env
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
LOCAL_EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
LOCAL_EMBEDDING_PYTHON=python
```

`.gitignore` 已忽略 `.env.local` 和 `data/generated/tea-vector-index.json`。当前没有 `NEXT_PUBLIC_DEEPSEEK_API_KEY`。真实 Key 不应写入 README、React Client Component、日志、测试或 Git。

**验证状态：NOT VERIFIED。** 本机无 `.env.local`，进程环境也无 `DEEPSEEK_API_KEY`，所以没有发出真实 DeepSeek 请求、没有验证模型可用性、JSON 输出兼容性、超时与实际响应质量。

### 6.2 Local Open-Source Embedding 与 Vector Index

- Node 侧文件：`lib/rag/local-embeddings.ts`。
- Python runner：`scripts/local-embed.py`。
- Python 依赖：`scripts/requirements-local-embeddings.txt`，内容为 `sentence-transformers>=3.0,<4.0`。
- 默认模型：`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`。
- 建索引：`scripts/build-tea-index.cjs` 调用本地 runner，对所有知识块生成 embedding，写入 `data/generated/tea-vector-index.json`。
- Query embedding：`enhanceWithLiveRag()` 在运行时调用同一个本地 runner。
- 向量格式：JSON，含 `version`、`model`、`dimensions` 和每个 chunk 的 `embedding: number[]`。
- 检索文件：`lib/rag/vector-store.ts`。
- 方法：真实 cosine similarity；`topK = 5`，`threshold = 0.28`。额外对匹配 product/category 加小幅业务 boost。
- 模型权重：代码不把权重写入仓库；Sentence Transformers 首次实际执行通常会下载并缓存到本机环境。生成 index 已被 Git ignore。

**验证状态：NOT VERIFIED。** 当前本机：

- 没有安装 Python 模块 `sentence_transformers`。
- 不存在 `data/generated/tea-vector-index.json`。
- 没有运行 `npm run build:tea-index`。
- `test:rag` 使用的是 2 维内存 stub vector index，验证 cosine / category retrieval 与若干 guard；不是对真实模型和真实索引的端到端验证。

因此必须明确写为：**尚未完成真实 Local Embedding 验证。**

## 7. Grounded Generation、Validator、Citation Filter 与 Fallback

### Hybrid RAG 职责边界

Structured Engine 持续负责以下事实与规则，不交给模型猜测：

- SKU / 商品是否存在、规格、包装、价格性质与金额。
- 预算过滤、数量 × 单价、最多 / 最低 / 最高、价格比较与价格反查。
- 负向偏好、茶类 hard filter、商品推荐硬条件。
- privacy / authorization guard、医疗 / 保健拒答和明确业务边界。

Vector RAG / DeepSeek 仅被设计用于：风味、冲泡、商品解释、差异、适合人群、品牌、产区、储存与售后规则的自然组织。提示词明确限定为：

```text
STRUCTURED FACTS + RETRIEVED KNOWLEDGE
```

并禁止自行补充 SKU、价格、规格、包装、库存、优惠、发货、销量、医疗功效或外部销售数据。

### Validator 与 Citation Filter

`lib/rag/pipeline.ts` 的 `validateGroundedOutput()` 当前会：

1. 验证 JSON 结构和 `confidence` 枚举。
2. 只保留当前 retrieval 命中的 citation ID；无允许 citation 则拒绝 Live 输出。
3. 提取回答出现的金额；若不在 `STRUCTURED FACTS` 的允许价格中，则拒绝 Live 输出。

状态：

- Citation Filter：PASS（单元级 / stub 覆盖）。
- Validator：PARTIAL。当前只验证结构、允许引用和价格，不会语义级验证每一个风味、品牌或产区陈述；尚未对真实模型输出验证。
- Grounded Generation：NOT VERIFIED。真实 DeepSeek 调用未发生。

### Fallback 行为

| 条件 | API Route 返回模式 | 实际行为 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` 缺失 | `structured` | 不读取 index，不调用 Embedding / DeepSeek，直接返回 Phase 3.5 结构化结果。 |
| pending quantity dialog | `structured` | 保留多轮补槽，不进入 RAG。 |
| index 不存在 | `fallback` | 已有结构化回答继续返回。 |
| Local Embedding 失败 | `fallback` | catch 后返回既有结构化回答。 |
| retrieval 低于 threshold | `fallback` | 不调用 DeepSeek，返回既有结构化回答。 |
| DeepSeek 请求失败 / timeout / JSON parse 失败 | `fallback` | catch 后返回既有结构化回答。 |
| Validator / Citation Filter 拒绝输出 | `fallback` | 不使用模型输出，返回既有结构化回答。 |

UI：`components/tea-demo/TeaChat.tsx` 展示“实时 RAG / 结构化检索 / 本地兜底”。它先构造本地 `processTeaTurn()` 结果，再调用 API；网络异常时仍保留本地答案，因此当前代码路径不应白屏。**未做本轮浏览器人工验证。**

另有 `scripts/eval-tea-rag.cjs`，但它目前只打印固定的 `requires-live-index` 占位结果，不执行实际检索、DeepSeek 调用或质量评估；其文案仍提及“configured provider”，需在 Live 验证阶段一并审计和更新。

## 8. 当前测试总览

| 模块 | 状态 | 自动化覆盖 | 人工验证 | 备注 |
| --- | --- | --- | --- | --- |
| Price | PASS | 有 | 未做本轮浏览器验收 | 71/71 包含价格查询、反查、比较。 |
| SKU | PASS | 有 | 未做本轮浏览器验收 | 规格、包装和价格证据分离。250g 两项仍非正式 SKU。 |
| Budget | PASS | 有 | 未做本轮浏览器验收 | 预算筛选和无合适项有覆盖。 |
| Quantity | PASS | 有 | 未做本轮浏览器验收 | 无上下文、显式价格、最大数量、超预算 / 剩余有覆盖。 |
| Multi-turn | PASS | 有 | NOT VERIFIED | 500 → 明前龙井 → 60g 覆盖到 ¥218 / ¥282。 |
| Compound Query | PASS | 有 | NOT VERIFIED | 目标 418 + 最便宜礼盒问题覆盖；基于标点 segmentation。 |
| Negative Preference | PASS | 有 | NOT VERIFIED | 桂花 hard filter 覆盖。 |
| Privacy Guard | PASS | 有 | NOT VERIFIED | “别的客人订单”覆盖。 |
| Medical/Health Guard | PASS | 有 | NOT VERIFIED | “有保健作用吗”覆盖。 |
| Catalog Existence | PASS | 有 | NOT VERIFIED | “铁观音有吗”覆盖。 |
| Retrieval | PARTIAL | 有 | NOT VERIFIED | stub index 验证 cosine、category boost；真实 index 不存在。 |
| Local Embedding | NOT VERIFIED | 配置级断言 | NOT VERIFIED | 缺 Python 包、无模型、无 index。 |
| DeepSeek Provider | NOT VERIFIED | 无 Live mock / Live integration | NOT VERIFIED | 代码路径存在，未配置 Key。 |
| Grounded Generation | NOT VERIFIED | 无 | NOT VERIFIED | 未真实调用 DeepSeek。 |
| Validator | PARTIAL | 有 | 不适用 | 覆盖错误价格拒绝；非价格事实未作全语义验证。 |
| Citation Filter | PASS | 有 | NOT VERIFIED | 只允许 retrieval 返回的 KB ID。 |
| Fallback | PARTIAL | 间接覆盖 | NOT VERIFIED | 代码分支清晰，但未逐项端到端模拟所有运行时失败。 |
| Build | PASS | `npm run build` | 不适用 | 本次成功。 |

## 9. 是否可封板

### Phase 3.5

**当前结论：PARTIAL，暂不正式宣称封板。**

理由：核心自动化逻辑 71/71 和 build 均通过，且目标多轮、compound、负向偏好、价格与三个 guard 都有真实测试覆盖；但本次没有在浏览器人工走完关键聊天路径、来源展示与错误状态。若下一窗口完成这轮人工验收且未发现核心 bug，可据此决定正式封板。

### Phase 4.0

**当前结论：代码骨架与安全 fallback 已实现，真实运行验证尚未开始。绝不能宣称封板。**

目前已完成：Next.js Server Route、DeepSeek provider 配置、Local embedding runner、离线 index builder、cosine Top-K、grounded prompt、基础 validator、citation filter、Phase 3.5 fallback 和状态 UI。

尚未完成验证：本地模型安装/下载、真实索引生成、query embedding、真实 retrieval、真实 DeepSeek 调用、Live grounded generation、真实 citation/validator/fallback 行为、RAG eval。

## 10. 下一窗口 TODO（按优先级）

1. 先读取本文档，再执行 `git status --short --branch` 和 `git rev-parse HEAD`，确认状态没有变化。
2. 先完成 Phase 3.5 浏览器人工验收，不改 UI：至少走一遍 500 → 明前龙井 → 60g、418 compound、负向桂花、铁观音、保健、客户订单，以及来源面板和错误状态。若出现核心 bug，先修复 Phase 3.5 → 回归 → build → commit → push；在此之前不继续 Phase 4.0。
3. 若 Phase 3.5 人工验收通过，再在 `.env.local` 配置真实 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`。Key 只能留在本机，不能展示、提交或写进前端。
4. 安装本地 embedding 依赖：`python -m pip install -r scripts/requirements-local-embeddings.txt`。确认模型首次下载后只使用本地缓存；不要把权重写进仓库。
5. 运行 `npm run build:tea-index`，确认生成 `data/generated/tea-vector-index.json`，检查模型名、维度、chunks 与 Git ignore。
6. 用真实 index 验证 query embedding、cosine Top-K 和 `threshold = 0.28`；再做 DeepSeek Live RAG、Grounded Generation、Validator、Citation Filter、每类 fallback 以及实际 RAG eval。
7. Live RAG 完整通过后再运行 `npm run test:tea`、`npm run test:rag`、`npm run build`，审阅 `git diff`、`git status`，然后才考虑 commit / push。

## 11. 给新 ChatGPT 窗口的启动语

> 按仓库根目录的《AI售前网站_当前项目最新交接_2026-08-31.md》继续，不要从零重写。先读取当前真实 Git 状态，严格区分已完成、部分完成和未验证内容。优先完成 Phase 3.5 Gate 的浏览器人工验收；若 Gate 无核心问题，再按交接文档验证当前 Phase 4.0 的 DeepSeek + Local Embedding 实现。不要因为代码已提交或部分测试通过就认为 Phase 4.0 已封板。

## 12. 最终状态摘要

```text
Current Branch: main
Current HEAD: 3f3f72d2200d42d010d868bb9202f31e77aced8a
Latest Commit: 3f3f72d fix: align rag with DeepSeek and local embeddings
Working Tree: 校验时 clean；生成本文档后只有本文档待提交
Remote Push Status: main 与 origin/main 同步（校验时）
```
