# AI 售前网站｜当前项目交接（Phase 6）

> 本文是当前唯一的项目状态交接文档。Phase 3.5 文档仅供历史追溯。

## 1. 项目定位

这是一个面向 AI 售前 / 解决方案工程师求职场景的作品集网站。核心以真实可交互 POC 展示 AI 售前方案理解、AI 导购 / 知识库、招投标 Agent、RAG / Hybrid Retrieval、Agent 工作流、Evidence / Trace 与业务分析能力。

## 2. 当前导航结构

- 首页（`/`）
- 在线简历（`/resume`）
- AI 导购（`/demo/tea-assistant`）
- 招投标 Agent（`/demo/tender-agent`）
- 方案展示（`/solutions`）

独立“关于我”已取消，相关内容整合至在线简历；首页“了解我”跳转在线简历。

## 3. AI 导购当前状态

一叶春山知识库 POC 使用独立的 `data/tea/**` 数据空间。当前能力包括商品问答、需求识别、预算 / 送礼对象 / 风味偏好解析、结构化规则匹配、商品推荐、产品资料检索、RAG / Knowledge Base、参考资料引用与可追溯回答。

服务端先执行本地规则与知识边界判断；配置 `DEEPSEEK_API_KEY`、命中适用意图且向量索引可用时，再以少量相关 Evidence 组织实时 RAG 回答。价格、规格、包装等结构化事实优先，模型输出会做引用与事实校验；不满足条件或 Provider 失败时保留本地结构化结果。前端展示 AI 执行过程、商品知识来源与本轮资料引用。

静态茶叶向量索引位于 `data/generated/tea-vector-index.json`，进程内缓存后复用；每次提问只对 query 做 embedding 与检索。当前 POC 不连接真实订单、库存、支付或物流系统，所有答案受已收录项目资料边界限制。

## 4. 招投标 Agent 当前状态

当前实现链路为：上传资料 → 文档解析 → 必要时 OCR → 文本处理 / 结构化要求抽取 → Hybrid RAG → `knowledge_search` → 按需 `web_search` / Tavily → Tender Analysis Agent → Evidence / Agent Trace → 综合判断与分析输出。

可分析招标文件与企业资料，并输出可投性判断、资格要求、技术与案例匹配、评分项、风险、缺失资料、投标建议与来源 Evidence。Agent 的执行记录来自真实的文档解析、检索、工具调用与规则 / 模型结果，不展示内部推理。所有企业资料、案例与判断仍属于 POC 演示，最终资格、承诺与投标决策必须人工复核。

## 5. 当前技术架构

- Next.js 15、React 19、TypeScript、Tailwind CSS
- DeepSeek（服务端 `DEEPSEEK_API_KEY`）
- 本地 Hybrid RAG：关键词检索、向量检索与 RRF 融合
- sentence-transformers `paraphrase-multilingual-MiniLM-L12-v2`，384 维 embedding
- 文档解析：PDF、DOCX（Mammoth）、TXT / Markdown；OCR 能力按环境配置启用
- Tavily 外部检索（`TAVILY_API_KEY`）
- Evidence、来源引用、Agent Trace 与人工复核边界

## 6. 方案展示页面

`/solutions` 已按“项目 / 方案思考 / 业务材料”分层，包含核心 POC、售前方案方法论、从业务问题到 POC、技术选型与方案架构、竞品与替代方案、ROI、POC 验证、关键决策、失败复盘、业务资料与交付材料，以及 Delivery Center 收尾 CTA。页面内二级锚点导航覆盖核心 POC、方案设计、业务判断、价值验证、项目复盘与交付材料。

两份产品手册均以在线连续 PDF 阅读呈现，支持缩放、页码识别与平滑翻页，不提供显式下载入口。方案内容与视觉已完成主要收口；后续仅需针对真实部署环境做验收和细节校对。

## 7. 当前项目架构图

已在方案展示页实现三张代码原生架构图，组件为 `components/solutions/SolutionArchitectureDiagrams.tsx`：

- AI 导购系统架构图
- 招投标 Agent 系统架构图
- 招投标 Agent 单次任务执行流程图

它们不是 `public/` 下的位图资源，而是随页面渲染的 React 组件。

## 8. 测试与构建状态

本轮重新执行：

```bash
npm run test:tea
npm run test:rag
npm run test:tender
npm run smoke:tender-rag
npm run build
```

本轮结果：`test:tea` 76/76 通过，`test:rag` 25/25 通过，`test:tender` 57/57 通过，`smoke:tender-rag` 确认 Hybrid RAG / RRF 正常，`npm run build` 通过。Tender 测试在未提供可用外部模型网络时会记录 Planner fetch fallback 日志，断言仍通过并走可审计回退。

AI 导购本地接口复测（预热后、含 Provider 完整响应）：桂花红茶适配问题约 3003ms，预算推荐约 2352ms，龙井冲泡约 1445ms；三轮均返回 `live-rag` 与资料引用。开发环境会输出不含问题内容的阶段耗时日志；当前 Provider 仍返回受校验的完整 JSON，因此该数值是完整回答时间，不应误标为首 token 时间。

## 9. 部署与 GitHub

- GitHub remote：`https://github.com/neyoxxx16-maker/ai-presales-portfolio.git`
- 主分支：`main`
- 最近 Phase 6 checkpoint：`e739b0e4b067e1fc5e6b7135d29444ff22d1453a`（`feat: complete phase 6 portfolio experience`）
- 未在仓库中确认可作为事实记录的 Vercel 生产部署状态；部署前应以 Vercel 项目设置为准。

密钥仅放入本地 `.env.local` 或部署平台环境变量，且绝不写入本文件。相关变量名包括：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`、`TAVILY_API_KEY`、`LOCAL_EMBEDDING_MODEL`、`LOCAL_EMBEDDING_PYTHON`。

## 10. 当前剩余待办

### P0

- 在目标部署环境完成 AI 导购与招投标 Agent 的端到端验收，确认密钥、OCR、Tavily 与本地 / 可替代 embedding 运行条件。
- 对真实示例资料完成一次人工核验，确保 Evidence、页码和风险结论的呈现边界清晰。

### P1

- 收集 AI 导购三类典型问题的真实响应耗时，持续评估首屏等待与 Provider 波动。
- 完成 GitHub / Vercel 最终部署检查，并复核简历、作品集与外部入口。

### P2

- 仅在真实验收提出明确问题时，做页面视觉和文案的最后细节收口。
- Phase 7 暂无独立功能范围；是否开启取决于部署验收反馈，而非预设重构计划。
